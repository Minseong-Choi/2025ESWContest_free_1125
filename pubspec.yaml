name: dementia_prevention_app
description: A drawing app for dementia prevention with EV3 robot integration
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.0.7
  audioplayers: ^6.5.0

  # UI 관련
  cupertino_icons: ^1.0.6

  # 네트워크 통신 (추후 백엔드 연결 시 사용)
  http: ^1.1.0

  # Bluetooth 통신 (실제 EV3 연결 시 사용)
  # flutter_blue_plus: ^1.31.15

  # 권한 관리 (실제 Bluetooth 사용 시 필요)
  # permission_handler: ^11.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true

  # 폰트 설정 (옵션)


  # 이미지 assets (필요시)
  # assets:
  #   - assets/images/
  #   - assets/icons/